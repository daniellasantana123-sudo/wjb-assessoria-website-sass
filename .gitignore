# dependencies
node_modules

# next.js build output
.next
out

# testing artifacts
test-results
playwright-report
coverage
*.tsbuildinfo

# env files - tudo com segredo real fica de fora; .env.example é o único
# arquivo de env com intenção de ser versionado. Padrão amplo (`.env*`)
# em vez de listar variante por variante (Checkpoint 6.5.1, 2026-09-20 -
# a lista anterior, herdada da Fase 6.5, não cobria .env.test/.env.test.local
# e exigiria lembrar de atualizar a cada variante nova).
.env*
!.env.example

# macOS
.DS_Store

# misc
*.log
LOG_imapsync/

# config local do Claude Code (skills, locks) - específico da máquina
.claude/
