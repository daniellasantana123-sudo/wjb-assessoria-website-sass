# dependencies
node_modules

# next.js build output
.next
out

# testing artifacts
test-results
playwright-report
coverage
*.tsbuildinfo

# env files - tudo com segredo real fica de fora; .env.example é o único
# arquivo de env com intenção de ser versionado (auditoria Fase 6.5,
# 2026-09-20 - `.env*.local` sozinho não cobria .env/.env.production puros)
.env
.env.local
.env.production
.env.production.local
.env.development.local
!.env.example

# macOS
.DS_Store

# misc
*.log
LOG_imapsync/

# config local do Claude Code (skills, locks) - específico da máquina
.claude/
